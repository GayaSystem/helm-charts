apiVersion: v1
kind: Service
metadata:
  name: {{ include "lewindigo.fullname" . }}
  labels:
    {{- include "lewindigo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lewindigo.selectorLabels" . | nindent 4 }}

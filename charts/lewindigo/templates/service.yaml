apiVersion: v1
kind: Service
metadata:
  name: lewindigo-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: web-http
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
  type: ClusterIP